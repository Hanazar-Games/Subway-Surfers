{
  "name": "subway-surfers",
  "private": true,
  "scripts": {
    "build": "node scripts/build-pages.mjs",
    "test": "node --test tests/*.test.cjs",
    "test:browser": "node tests/browser.mjs && node tests/browser-lifecycle.mjs"
  },
  "devDependencies": {
    "playwright": "1.57.0"
  }
}
